<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}" />
</head>
<header class="header">
    <div class="container_header">
        <span class="header_logo"><img class="header_logo_img" src="static/book_logo.png" alt="book logo"></span>
        <span class="header_menu"><a class="header_menu" href="/">Home</a></span>
    </div>
</header>

<div class="hero_text">
    {# new & trending #}
    <h1 class="hero__title_text">{{ title }}</h1>
    <h3 class="subtitle_text">Our most borrowed books this month.</h3>
    <hr>
    {# homepage 3 buttons #}
    <div class="container_button">
        <button class="menu_button" onclick="location.href='/'" type="button">Home</button>
        <button class="menu_button" onclick="location.href='/books'" type="button">Books</button>
        <button class="menu_button" onclick="location.href='/books/new'" type="button">Add Book</button>
    </div>
</div>
<main>
    <section class="hero">
        <div class="container hero_wrapper">
            {% block content %}

            {% endblock content %}
        </div>
    </section>
</main>

</html>